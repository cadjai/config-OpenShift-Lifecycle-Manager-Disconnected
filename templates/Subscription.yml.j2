apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: {{ operator_name }}
  namespace: {{ operator_target_namespace | d('openshift-operators') }}
spec:
  channel: alpha
  name: {{ operator_name }}
  source: {{ operator_catalog_name }}
  sourceNamespace: openshift-marketplace
