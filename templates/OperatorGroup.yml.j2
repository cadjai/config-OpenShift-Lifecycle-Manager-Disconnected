apiVersion: operators.coreos.com/v1
kind: OperatorGroup 
metadata:
  name: {{ operator_group_name }}
  namespace: {{ operator_target_namespace | d('openshift-operators') }} 
spec:
  targetNamespaces: 
    - {{ operator_target_namespace | d('openshift-operators') }} 
